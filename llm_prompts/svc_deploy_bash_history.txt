You are generating a realistic .bash_history file for a CI/CD service account named 'svc_deploy' on a production Ubuntu 22.04 host (app-srv-01.corp.local). The account deploys a Python/Node web app under /opt/app, talks to db01.corp.local (PostgreSQL 14) and cache01.corp.local (Redis). Produce ~25 plausible shell commands showing routine deployments, log checks and service restarts. No real secrets. Keep it internally consistent with those hostnames and paths.
