# LLM-Enhanced Honeypot Analytics - Python dependencies
# Python 3.10+ (δοκιμασμενο σε 3.10 και 3.12)
#
# Εγκατασταση:
#   python3 -m venv venv
#   source venv/bin/activate            # Windows: venv\Scripts\activate
#   pip install -r requirements.txt

paramiko>=3.4           # πραγματικο SSH transport για το honeypot (Task 1/3)
scikit-learn>=1.3       # KMeans, DBSCAN, IsolationForest, PCA, t-SNE, RandomForest (Task 5, optional)
numpy>=1.24
pandas>=2.0
matplotlib>=3.7         # figures (Task 5/6)
