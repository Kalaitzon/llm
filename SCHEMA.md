# ── Kalaitzidis Ioannis - MTE25012 ─────────────────────────────────────────────────────

# Σχημα καταγραφης συνεδριων (Session Log Schema)

## Τι ειναι αυτο το αρχειο και γιατι υπαρχει

Καθε honeypot εχει αξια μονο αν καταγραφει με ακριβεια τι εκανε ο επιτιθεμενος.
Το αρχειο αυτο περιγραφει τη ΜΟΡΦΗ (schema) με την οποια το honeypot
(`02_ssh_honeypot.py`) αποθηκευει καθε συνεδρια, ωστε:

- να ειναι σαφες σε οποιονδηποτε διαβασει τα logs τι σημαινει καθε πεδιο,
- να μπορει καθε συνεδρια να ανακατασκευαστει πληρως (δηλαδη να ξαναδουμε βημα
  προς βημα τι εντολες εδωσε ο επιτιθεμενος, με ποια σειρα, και τι διαβασε),
- να μπορει το προγραμμα αναλυσης (`04_analyze_sessions.py`) να διαβασει τα
  δεδομενα με προγραμματιστικο, αξιοπιστο τροπο.

## Γιατι η καταγραφη γινεται σε δυο μορφες

Η καταγραφη γινεται ταυτοχρονα σε δυο σημεια, γιατι εξυπηρετουν διαφορετικες
αναγκες:

- **`logs/sessions/session_<id>.json`** : ενα αυτονομο, ευαναγνωστο (indented)
  αρχειο JSON ανα συνεδρια. Ειναι βολικο για να ανοιξει καποιος ΜΙΑ συγκεκριμενη
  συνεδρια και να τη μελετησει με το ματι.
- **`logs/sessions.jsonl`** : ενα ενιαιο master αρχειο σε μορφη JSON Lines,
  οπου καθε γραμμη ειναι μια πληρης συνεδρια. Ειναι ιδανικο για ΜΑΖΙΚΗ αναλυση,
  καθως το προγραμμα αναλυσης το διαβαζει γραμμη προς γραμμη, χωρις να χρειαζεται
  να ανοιξει εκατονταδες ξεχωριστα αρχεια.

Το περιεχομενο ειναι το ιδιο και στις δυο μορφες. Αλλαζει μονο η οργανωση.

## Πληρες παραδειγμα μιας συνεδριας

```json
{
  "session_id": "a1b2c3d4e5f6",
  "src_ip": "127.0.0.1",
  "src_port": 51566,
  "start_time": "2026-04-20T09:15:03.120000+0000",
  "end_time":   "2026-04-20T09:15:10.640000+0000",
  "duration_seconds": 7.52,
  "client_version": "SSH-2.0-paramiko_3.4.0",
  "auth": {
    "username": "root",
    "success": true,
    "attempts": [
      {"time": "...", "username": "root", "method": "password",
       "credential_sample": "123456"},
      {"time": "...", "username": "root", "method": "password",
       "credential_sample": "Corp2026!"}
    ]
  },
  "command_count": 6,
  "commands": ["cd /opt/app", "cat .env", "grep -r password /opt/app", "..."],
  "artefacts_touched": ["/opt/app/.env", "/opt/app/config/database.yml"],
  "events": [
    {
      "seq": 1,
      "time": "2026-04-20T09:15:03.500000+0000",
      "type": "command",
      "input": "cat .env",
      "cwd": "/opt/app",
      "output_bytes": 412,
      "artefacts_touched": ["/opt/app/.env"]
    }
  ]
}
```

Στο παραπανω παραδειγμα φαινεται μια συνεδρια οπου ο επιτιθεμενος (α) δοκιμασε
πρωτα εναν λανθασμενο κωδικο (123456) και μετα τον σωστο, (β) πηγε στον φακελο
της εφαρμογης, (γ) διαβασε το αρχειο .env και (δ) εκανε αναζητηση για κωδικους,
αγγιζοντας συνολικα δυο δολωματα.

## Περιγραφη των πεδιων της συνεδριας

| Πεδιο | Τυπος | Περιγραφη |
|-------|-------|-----------|
| `session_id` | string | Μοναδικο αναγνωριστικο συνεδριας (12 δεκαεξαδικοι χαρακτηρες). |
| `src_ip`, `src_port` | string / int | Διευθυνση και θυρα του πελατη. Το `src_port` ειναι μοναδικο ανα συνδεση και χρησιμοποιειται για την αντιστοιχιση της συνεδριας με το ground truth των playbooks (ποιο playbook την παρηγαγε). |
| `start_time`, `end_time` | ISO-8601 | Χρονικες σφραγιδες εναρξης και ληξης (σε UTC). |
| `duration_seconds` | float | Η διαρκεια της συνεδριας. Αντιστοιχει στη μετρικη dwell time της αναλυσης. |
| `client_version` | string | Το banner του SSH client του επιτιθεμενου (π.χ. το εργαλειο που χρησιμοποιει). Χρησιμο για fingerprinting του ιδιου του αντιπαλου. |
| `auth.username` | string | Το ονομα χρηστη με το οποιο εγινε η (επιτυχης) συνδεση. |
| `auth.success` | bool | Αν η συνδεση ολοκληρωθηκε. Οι συνεδριες με `false` και `command_count = 0` ειναι failed-auth probes, δηλαδη αποτυχημενες προσπαθειες εισοδου. |
| `auth.attempts` | list | Ολες οι προσπαθειες authentication της συνεδριας. Καταγραφεται μονο ενα **δειγμα** του credential, ποτε ολοκληρο, για λογους ασφαλειας. |
| `command_count` | int | Πληθος εντολων που εκτελεστηκαν. |
| `commands` | list[str] | Οι εντολες, με τη σειρα που δοθηκαν. |
| `artefacts_touched` | list[str] | Τα **μοναδικα** decoy artefacts που "αγγιξε" (διαβασε) ο επιτιθεμενος σε ολη τη συνεδρια. Ειναι η πραγματικη μετρικη engagement, δηλαδη ενα αντικειμενικο μετρο του ποσο βαθια ενεπλακη ο επιτιθεμενος. |
| `events` | list[obj] | Η αναλυτικη χρονικη ακολουθια των συμβαντων (βλ. παρακατω). |

## Περιγραφη των πεδιων ενος event

Καθε event αντιστοιχει σε μια εντολη και επιτρεπει την πληρη, βημα-προς-βημα
ανακατασκευη της συνεδριας.

| Πεδιο | Τυπος | Περιγραφη |
|-------|-------|-----------|
| `seq` | int | Αυξων αριθμος του συμβαντος εντος της συνεδριας (1, 2, 3, ...). |
| `time` | ISO-8601 | Η χρονικη σφραγιδα του συμβαντος. |
| `type` | string | Ο τυπος του συμβαντος (εδω παντα `command`). |
| `input` | string | Η ακριβης εντολη που εδωσε ο επιτιθεμενος. |
| `cwd` | string | Ο τρεχων καταλογος τη στιγμη της εντολης. Το honeypot κραταει αυτη την κατασταση ανα συνεδρια, ωστε οι διαδοχικες εντολες `cd` και `ls` να συμπεριφερονται οπως σε πραγματικο κελυφος. |
| `output_bytes` | int | Το μεγεθος της αποκρισης σε bytes (ενδειξη του ποσο "πλουσια" ηταν η εξοδος). |
| `artefacts_touched` | list[str] | Ποια δολωματα αγγιξε η ΣΥΓΚΕΚΡΙΜΕΝΗ εντολη. Μια εντολη οπως `grep -r` μπορει να αγγιξει πολλα δολωματα ταυτοχρονα, και αυτο καταγραφεται με ακριβεια. |

## Πως γινεται η ανακατασκευη μιας συνεδριας

Για να δει καποιος τι εκανε ενας επιτιθεμενος, αρκει να διαβασει τη λιστα
`events` με τη σειρα του `seq`: καθε event δειχνει την εντολη (`input`), το που
βρισκοταν (`cwd`), και τι δολωμα διαβασε (`artefacts_touched`). Το αθροισμα
ολων των `artefacts_touched` δινει τη συνολικη εικονα του τι πληροφορια απεκτησε
ο επιτιθεμενος.

## Σημειωση ασφαλειας

Δεν καταγραφονται ποτε ολοκληρα credentials, ουτε υπαρχουν πραγματικα μυστικα
στο εικονικο συστημα. Το honeypot ακουει μονο στο loopback (127.0.0.1).